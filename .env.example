# Traces — API Keys
# Optional fallbacks for the API-key providers only. In the app, configure keys via Settings > AI & Models.
# Codex, Grok CLI, and Claude sign-in do not use these variables. Do not put tokens in git.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
GOOGLE_API_KEY=
